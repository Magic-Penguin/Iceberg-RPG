const player=document.getElementById('player');const toast=document.getElementById('toast');let x=50,y=49;let attacking=false;function notify(t){toast.textContent=t;toast.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>toast.classList.remove('show'),1800)}function render(){player.style.left=x+'%';player.style.top=y+'%'}const keys=new Set();addEventListener('keydown',e=>{keys.add(e.key.toLowerCase());if(e.code==='Space'){e.preventDefault();attack()}if(e.key.toLowerCase()==='i')notify('Inventory locked — gather resources to unlock it.');if(e.key.toLowerCase()==='f')notify('Faction network detected — choose your allegiance soon.');if(e.key.toLowerCase()==='e')notify('Nothing here yet. The frontier is waiting.')});addEventListener('keyup',e=>keys.delete(e.key.toLowerCase()));function attack(){if(attacking)return;attacking=true;player.querySelector('.blade').style.transform='rotate(112deg)';notify('⚔ ATTACK');setTimeout(()=>{player.querySelector('.blade').style.transform='rotate(34deg)';attacking=false},180)}function loop(){const speed=.22;if(keys.has('w'))y=Math.max(13,y-speed);if(keys.has('s'))y=Math.min(87,y+speed);if(keys.has('a'))x=Math.max(8,x-speed);if(keys.has('d'))x=Math.min(92,x+speed);render();requestAnimationFrame(loop)}document.querySelectorAll('.actions button').forEach(b=>b.addEventListener('click',()=>{const a=b.dataset.action;if(a==='combat')attack();else if(a==='inventory')notify('Inventory locked — gather resources to unlock it.');else notify('Faction network detected — choose your allegiance soon.')}));notify('Welcome to the frontier. Stay alive.');loop();