const player=document.getElementById('player');const mapWorld=document.getElementById('mapWorld');const toast=document.getElementById('toast');const zoneEl=document.getElementById('zone');const mapZone=document.getElementById('mapZone');const coords=document.getElementById('coords');const youDot=document.getElementById('youDot');let px=1800,py=1200;let lastTime=performance.now();const keys=new Set();let mouseX=innerWidth/2,mouseY=innerHeight/2;function notify(t){toast.textContent=t;toast.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>toast.classList.remove('show'),1800)}function clamp(v,a,b){return Math.max(a,Math.min(b,v))}function camera(){const w=innerWidth,h=innerHeight;const camX=clamp(px-w/2,0,3600-w),camY=clamp(py-h/2,0,2400-h);mapWorld.style.transform=`translate(${-camX}px,${-camY}px)`;player.style.left=`${px-camX}px`;player.style.top=`${py-camY}px`;const gx=Math.round(px/10),gy=Math.round(py/10);coords.textContent=`X ${String(gx).padStart(3,'0')} · Y ${String(gy).padStart(3,'0')}`;const zone=px<1200?'ICE FIELDS':px<2300?'WHITEWATER':'OUTER SHELF';zoneEl.textContent=zone;mapZone.textContent=zone;youDot.style.left=`${clamp(px/3600*100,4,96)}%`;youDot.style.top=`${clamp(py/2400*100,4,96)}%`}function aim(){const rect=player.getBoundingClientRect();const cx=rect.left+rect.width/2,cy=rect.top+rect.height/2;const dx=mouseX-cx,dy=mouseY-cy;const distance=Math.hypot(dx,dy)||1;const eye=player.querySelector('.eye');eye.style.transform=`translate(${clamp(dx/distance*5,-5,5)}px,${clamp(dy/distance*5,-5,5)}px)`}addEventListener('mousemove',e=>{mouseX=e.clientX;mouseY=e.clientY;aim()});addEventListener('resize',aim);addEventListener('mousedown',e=>{if(e.button===0)throwSnowball()});addEventListener('keydown',e=>{keys.add(e.key.toLowerCase());if(e.key.toLowerCase()==='i')notify('Inventory locked — gather resources to unlock it.');if(e.key.toLowerCase()==='f')notify('Faction network detected — choose your allegiance soon.');if(e.key.toLowerCase()==='e')notify('Move closer to points of interest to interact.')});addEventListener('keyup',e=>keys.delete(e.key.toLowerCase()));function throwSnowball(){const rect=player.getBoundingClientRect();const sx=rect.left+rect.width/2,sy=rect.top+42;const dx=mouseX-sx,dy=mouseY-sy;const len=Math.hypot(dx,dy)||1;const ball=document.createElement('div');ball.className='snowball';ball.style.left=sx+'px';ball.style.top=sy+'px';document.querySelector('.game').appendChild(ball);let x=sx,y=sy,life=0;function fly(){x+=dx/len*13;y+=dy/len*13;life++;ball.style.left=x+'px';ball.style.top=y+'px';if(life<55&&x>-30&&x<innerWidth+30&&y>-30&&y<innerHeight+30)requestAnimationFrame(fly);else ball.remove()}requestAnimationFrame(fly)}document.querySelectorAll('.actions button').forEach(b=>b.addEventListener('click',()=>{const a=b.dataset.action;if(a==='inventory')notify('Inventory locked — gather resources to unlock it.');else notify('Faction network detected — choose your allegiance soon.')}));function loop(now){const dt=Math.min((now-lastTime)/16.67,2);lastTime=now;const speed=4.2*dt;if(keys.has('w'))py-=speed;if(keys.has('s'))py+=speed;if(keys.has('a'))px-=speed;if(keys.has('d'))px+=speed;px=clamp(px,40,3560);py=clamp(py,40,2360);camera();aim();requestAnimationFrame(loop)}camera();aim();notify('WASD to move · left click throws snowballs · mouse to look.');requestAnimationFrame(loop);